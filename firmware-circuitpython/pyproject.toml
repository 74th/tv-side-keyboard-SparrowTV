[project]
name = "firmware-circuitpython"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "adafruit-circuitpython-typing==1.9.4",
]

[dependency-groups]
dev = [
    "invoke~=2.2.0",
    "requests~=2.31.0",
]
